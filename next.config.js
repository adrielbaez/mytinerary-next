/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images6.alphacoders.com", "images.pexels.com"],
  },
};

module.exports = nextConfig;
